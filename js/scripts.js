$(document).readyfunction(event) {
    function resetFields() {
      var playersDetail = [player - 1, player - 2]
      $("input#new-player-1-name").val("");
      $("input#new-player-2-name").val("");
      $(button# newGame).click(function() {
          $("#newGame").append("players")
        }
        var dice = [1, 2, 3, 4, 5, 6]
        $(button# tose).click()
      }